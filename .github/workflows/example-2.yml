name: my first workflow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "Hello"

      - name: read README file
        run: cat README.md
        
      - name: List
        run: ls

