name: my first workflow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "Hello"

      - name: List
        run: ls

      - name: read README file
        run: cat README.md